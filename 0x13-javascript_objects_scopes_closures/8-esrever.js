#!/usr/bin/node
/*
 * A function that returns a reversed list without using `reversed`
 */
exports.esrever = function (list) {
  const reversedList = [];
  for (let i = list.length - 1; i >= 0; i--) {
    reversedList.push(list[i]);
  }
  return reversedList;
};
